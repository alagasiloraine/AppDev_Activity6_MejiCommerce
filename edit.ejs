<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/styles/style.css">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/edit/<%= product.id %>" method="POST">
        <input type="text" name="name" value="<%= product.name %>" required>
        <input type="number" name="price" value="<%= product.price %>" required>
        <textarea name="description"><%= product.description %></textarea>
        <input type="number" name="stocks" value="<%= product.stocks %>" required> <!-- New input for Stocks -->
        <button type="submit" class="button"><i class="fas fa-edit"></i>Update Product</button>
    </form>
    <a href="/" class="button button-back"><i class="fas fa-arrow-left"></i>Back to Inventory</a>

    <!-- Footer -->
    <footer>
        <p>&copy; Mejico Medical Spa 2024</p>
    </footer>
</body>
</html>